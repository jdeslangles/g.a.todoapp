<%= form_for(@to_do_item) do |f| %>
  <% if @to_do_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@to_do_item.errors.count, "error") %> prohibited this to_do_item from being saved:</h2>

      <ul>
      <% @to_do_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :position %><br />
    <%= f.number_field :position %>
  </div>
  <div class="field">
    <%= f.label :to_do_list_id, "List" %><br />
    <%= f.select(:to_do_list_id, ToDoList.all.map{|l| [l.name, l.id]}) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
